[project]
name = "iceberg-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
iceberg-demo = "iceberg_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
